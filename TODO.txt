Roadmap:

- Partial PSI-Walking - as described by you above and may replace the pure string analysis option (maybe statically via configuration or - if possible - dynamically based on some pre-processing logic)

Approach-unspecific features like:

- Obfuscation "Themes" (for one class/file, obfuscate with related but basic terms (customer, order, productId, price) based on very basic semantics)
- probably more QOL stuff to come





current:
reuse replacement names/numbers when old ones go out of scope (maybe only for indexers/identifiers)
change whitelists to json config files with links in the settings menu instead of raw list.